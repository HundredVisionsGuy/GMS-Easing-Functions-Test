/// @description create

// position variables
start = x;
dest = x + 500;
startY = y;
destY = y + 500;

// timing variables
duration = 120; // 2 seconds at 60 FPS
yDuration = duration / 2; // half the x duration
time = 0;

// rotation variables
startAngle = 0;
destAngle = 360;